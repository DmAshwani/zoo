server:
  port: 8081

spring:
  application:
    name: identity-service
  datasource:
    url: jdbc:postgresql://localhost:5432/zoo_identity_db
    username: zoo_admin
    password: ${DB_PASSWORD:zoo_password}
    driver-class-name: org.postgresql.Driver
  
  flyway:
    enabled: true
    locations: classpath:db/migration

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

app:
  jwt:
    secret: ${JWT_SECRET:ZooIdentitySecretKey2026!@#DynamicRBAC_Security}
    expirationMs: 86400000 # 24 hours

logging:
  level:
    com.zoo.identity: DEBUG
    org.springframework.jdbc.core.NamedParameterJdbcTemplate: DEBUG
